# packages/ui

Shared UI components for the merchant-facing frontend, if/when the
component surface grows large enough to warrant extraction from
`apps/web`.

Status: placeholder — not needed until `apps/web` exists.
