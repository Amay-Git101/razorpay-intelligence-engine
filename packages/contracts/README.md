# packages/contracts

Shared type/schema contracts (canonical event, decision, policy result,
audit entry) intended to be used by both `apps/api` and `apps/web` once
those exist, so the two never drift on shape.

Status: placeholder — no schemas defined yet.
