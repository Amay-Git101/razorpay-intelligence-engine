# Foundation-stage placeholder. No application reads this file yet.
# Copy to .env locally and fill in real values when implementation begins.
# NEVER commit a real .env file. NEVER put real secrets in this example file.

# --- Database ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=razorpay_decision_intelligence
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# --- Cache / idempotency / rate limiting ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Razorpay (Test Mode only — never put Live Mode keys in this repo) ---
RAZORPAY_KEY_ID=rzp_test_placeholder
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# --- App ---
APP_ENV=development
LOG_LEVEL=info
