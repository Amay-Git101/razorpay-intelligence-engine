# tests/unit

Unit tests, mirroring the structure of `apps/api` and `apps/web` once
those exist. No test framework has been chosen/installed yet — that is
an implementation decision, not part of the foundation scaffold.
