# workflows

Bounded action workflows (e.g. the recovery-intervention flow) —
orchestration definitions consumed by the Action Orchestrator.

Status: placeholder — no workflows implemented. The first vertical
slice's recovery workflow is explicitly NOT a silent recharge; see
`docs/adr/0001-repository-foundation.md` for the current scope
boundary and the master handoff document for the agreed flow.
