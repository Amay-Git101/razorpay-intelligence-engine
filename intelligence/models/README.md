# intelligence/models

Trained model artifacts and model-definition code (candidate families:
XGBoost, LightGBM, Isolation Forest, calibrated classifiers — see
architecture contract §11). Exact model choice is not yet frozen.

Status: placeholder — no models trained yet.
