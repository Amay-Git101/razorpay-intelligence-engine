# intelligence/features

Feature engineering for the AI/ML layer (derived signals from the event
store: recency, retry counts, recoverability indicators, etc. — see
architecture contract §9–§11).

Status: placeholder — no features defined; exact feature set is
explicitly "not yet frozen" per the architecture contract §43.
