"""Read-only aggregation over already-persisted decisions/actions data.

See metrics.py module docstring for the full scope and the semantic
limitations every metric in this package deliberately preserves.
"""
