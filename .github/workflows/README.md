# .github/workflows

CI/CD pipeline definitions (architecture contract §33–§34: lint, type
check, tests, SAST, dependency scan, build). No workflow YAML files
exist yet — there is no application to run CI against.
